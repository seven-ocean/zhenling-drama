# 2026-04-19 工作日志

## TEST.md 全面重写

**任务**：基于项目当前代码实际状态，全面整理 TEST.md 测试报告

**完成内容**：
- 项目概览（技术栈/定位/端口）
- 系统架构：前端7页面+11 Controller+14 Service+3 Adapter+9张表+6配置类
- 完整 API 接口清单（60+端点，11个前端模块映射）
- 已修复 BUG 记录（BUG00001~16 全部 ✅）
- 测试用例：P0 冒烟测试 8 项 + P1 功能测试 10 项 + 回归测试 6 项
- 已知问题与技术债务：5 高优先级 + 4 中优先级 + 5 低优先级
- 数据迁移记录（Fire Emblem 暗黑龙与光之剑 + 补数 SQL）
- 测试环境检查清单（8项）

**输出文件**: `TEST.md` — 从旧的过时内容全面重写为基于代码实际情况的完整测试报告

---

## BUG00018~21 修复（5个未修复BUG全部处理）

**任务**：修复 TEST.md 中剩余的5个未修复BUG

### 修改文件清单

| 文件 | 变更类型 | 核心改动 |
|------|---------|---------|
| **FileStorageService.java** | 增强+新增方法 | ① guessTypeByFilename() 扩展名检测 ② uploadFile() 三级类型判断链(MIME→扩展名→前端type) ③ 新增 uploadBytes(byte[],filename,dramaId,type,mimeType) 支持字节数组上传到OSS/本地 |
| **ImageGenerationService.java** | 重构归档逻辑 | ① 三个生成方法(character/scene/grid)全部改调 archiveAiImage() ② archiveAiImage()：下载AI临时图→uploadBytes上传→返回永久URL Asset ③ saveAiUrlFallback() 降级方案 |
| **MiniMaxAdapter.java** | 参数调整 | 图片生成请求体新增 `"content_safe": false` 放宽内容审核 |
| **StorageSettings.vue** | 前端预览修复 | ① 新增 getRealFileType(item) 三级推断(mimeType→db type→扩展名) ② openPreview()+模板全部用getRealFileType替代item.type |
| **DramaDetail.vue** | 事件穿透修复 | 场景卡片删除按钮从内联改为独立div包裹+z-index提升+@click.stop阻止冒泡到openSceneModal |
| **TEST.md** | 状态更新 | BUG00018~21 全部标记 ✅已修复 + 详细方案说明 |

### 各BUG修复摘要
- **BUG00018**: 后端双重类型检测(扩展名兜底) + 前端真实类型推断(解决mp4被当image的问题)
- **BUG00018-1**: 删除按钮事件冒泡修复(场景卡片)
- **BUG00019**: 经查证后端saveBatch+前端loadData均正确，无需额外修复
- **BUG00020**: MiniMax content_safe=false 解决敏感内容误拦截
- **BUG00021**: AI生成图片自动下载并归档到用户OSS(含降级fallback)

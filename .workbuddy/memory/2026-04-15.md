# 2026-04-15 工作日志

## BUG00015 修复 - AI生成按钮 ofetch is not defined
**根因**: `ai.ts` 和 `media.ts` 直接调用裸 `ofetch()` 但未 import
**修复**: 改为 import `api` 实例（带 baseURL + 认证头），与其他模块保持一致
**涉及文件**: 
- `frontend/src/utils/ai.ts` — 添加 `import api from './request'`
- `frontend/src/utils/media.ts` — 同上（顺带修复同类隐患）

## DramaDetail.vue 图片按钮调整
用户要求保留3个按钮：从存储选择 / 上传图片 / AI生成（角色弹窗、场景弹窗、设置Tab封面均一致）


## UI 优化大升级（4项任务全部完成）

### 1. 全局按钮样式 + Ant Design 规范优化
- **App.vue** 全局样式从3行扩展到11大类（~120行CSS）：
  - 按钮系统：inline-flex + gap + transition 统一
  - Tag/Select/Modal/Form/Tooltip/Popconfirm/Empty/Spin/Alert/Scrollbar 全面暗色适配
- **TaskTracker.vue** 修复按钮不规范写法（`<ReloadOutlined />` → `<template #icon>`）
- **DramaDetail.vue** 修复 cancelText 引号错误 `"'取消'"` → `"取消"`

### 2. 分页优化（3个页面）
- **DramaList.vue**: 新增 a-pagination，pageSize 默认12，支持9/12/18/24
- **TaskTracker.vue**: 从固定50条改为分页20/30/50
- **StorageSettings.vue**: 从固定100条改为分页12/24/48/96
- 所有分页组件统一暗色样式（:deep 覆盖）

### 3. 角色/场景图片 — AI生成入口
- DramaDetail.vue 角色弹窗新增「AI生成」按钮（调用 aiApi.generateCharacterImage）
- 场景弹窗新增「AI生成」按钮（调用 aiApi.generateSceneImage）
- 生成中显示 loading 状态，自动填入 imageUrl

### 4. 剧集封面功能
- 设置 Tab 新增封面上传区：预览图 + 存储选择 + 直接上传 + 移除按钮
- 新建剧集表单也加了封面入口（点击上传）
- imagePicker 扩展支持 'cover' target 类型
- 后端无需改 DB（cover_image varchar(512) 已存在）

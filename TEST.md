# 这张表专门整理测试用例，跟进测试情况

-------------------------------全局提示词start-------------------------------

# 接下来的任务（后端）

在backend继续迭代功能

## 要求

1、要符合这个工程文件的目录结构和代码规范
2、要在这个工程文件的基础上继续迭代，不能完全重新开始
3、请勿重新编译项目验证修复

## 修复功能

# 接下来的任务（前端）

在frontend继续迭代功能

## 要求

1、要符合这个工程文件的目录结构和代码规范
2、要在这个工程文件的基础上继续迭代，不能完全重新开始
3、请勿重新编译项目验证修复

## 修复功能

-------------------------------全局提示词end-------------------------------



| **编号**   | **功能路径**                         | **预期结果**     | **实测结果**                                                                                                                                                                                                 | **是否修复** | **AI给出的修复方案** |
|----------|--------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------|----------|----------|
| **BUG00001** | **/drama/new (POST /api/v1/dramas)** | **可以创建剧集**   | **创建剧集失败：POST "/dramas": 404 Not Found**                                                                                                                                                                 | **已修复** | **① 执行 `backend/sql/init-full.sql` 初始化数据库表结构 + 示例数据；② 确保后端服务启动（端口8080）；③ 前端 vite.config.ts 已配置 proxy `/api → localhost:8080`。根因：数据库 `dramas` 表不存在或缺少 `deleted` 列（实体类有但原 init.sql 漏掉），导致后端报错返回404** |
| **BUG00002** | **/settings/ai**                 | **可以添加配置**   | **不知道怎么配置，需要初始化参数SQL参考数据**                                                                                                                                                                               | **已修复** | **已创建 `backend/sql/init-full.sql`，内含 4条AI配置示例（OpenAI图片/文本/MiniMax视频/TTS）+ 2条示例剧集 + 3个示例角色 + 3个示例场景。用户只需将 SQL 中的 `sk-your-openai-key-here` 替换为真实 API Key 即可使用。详见 init-full.sql 文件** |
| **BUG00003** | **/dramas**                      | **可以正常添加剧集** | **添加失败：POST http://localhost:5173/dramas 返回 404 Not Found。入参：{"title":"111","description":"111","totalEpisodes":1}**                                                                                     | **已修复** | **根因：`request.ts` 中 `ofetch.create({ baseURL })` 创建了带前缀的实例，但 `export default ofetch` 导出的仍是原始 ofetch（无 baseURL）。导致所有请求直接打 `/dramas` 而非 `/api/v1/dramas`，Vite 代理只转发 `/api` 开头路径 → 404。修复：将 create 实例赋值给 `const api` 并 `export default api`，所有 API 函数改用 `api()` 调用，请求自动携带 `/api/v1` 前缀** |
| **BUG00004** | **/ai-configs**                  | **可以正常添加AI服务配置** | **添加失败：POST http://localhost:5173/ai-configs 返回 404 Not Found。入参：{"provider":"openai","apiType":"image","priority":0,"enabled":true,"baseUrl":"1111111111111","apiKey":"1111111111","model":"11111111111"}** | **已修复** | **根因：同 BUG00003，`request.ts` 导出原始 ofetch 导致请求缺少 `/api/v1` 前缀。修复：同上，统一使用带 baseURL 的 api 实例，`aiConfig.ts` 通过 `import ofetch from './request'` 已自动获得正确实例** |
|**BUG00005**| **/dramas** | **可以看到数据** | **页面是空的，检查下是数据库没有数据，还是接口调用有问题，还是接口有问题** | **已修复** | **根因：① 后端可能未启动（端口8080）；② 数据库可能未执行初始化SQL（`zhenling_drama.sql`）；③ 前端错误处理静默（只有 `console.error`），用户无法区分"无数据"和"加载失败"。修复：DramaList.vue 增加 errorMsg 状态 + AMessage.error() 提示 + a-result 错误展示（含重试按钮）。兼容后端返回 IPage(records字段)或数组两种格式。用户现在能看到具体错误原因（如"网络异常，请检查后端服务是否启动"）** |
|**BUG00006**| **http://localhost:5173/settings/ai** | **可以看到数据** | **页面是空的，检查下是数据库没有数据，还是接口调用有问题，还是接口有问题** | **已修复** | **根因：① 同上，后端/数据库问题 + 错误静默；② 原代码只调用 `/ai-configs/type/image` 接口加载图片类型配置，其他类型(text/video/tts)完全不显示。修复：① 并行请求全部4种类型(text/image/video/tts)的配置，合并展示；② 增加 errorMsg 状态 + 错误反馈；③ 新增类型筛选 Tab 栏（全部/文本/图片/视频/语音合成），带数量角标；④ 空状态分两种："完全无数据"和"该类型暂无配置"** |
|**BUG00007**| **http://localhost:5173/drama/demo-drama-001** | **可以看到数据** | **页面是空的，检查下是数据库没有数据，还是接口调用有问题，还是接口有问题** | **已修复** | **根因：后端未启动时 API 报错 → catch 被静默吞掉 → store.currentDrama 为 null → 详情区域条件渲染判断不完整导致看起来空白。修复：① loadData() 增加 loadError 状态 + 具体错误提示；② 新增加载失败页面：a-result warning 状态 + 重试/返回列表按钮；③ 新增404保护：加载完成但 currentDrama 仍为 null 时显示"剧集不存在"；④ Tabs 区域增加 `!loadError && store.currentDrama` 条件防止渲染空内容** |
|**BUG00008**| **http://localhost:5173/drama/demo-drama-002** | **可以看到数据** | **页面是空的，检查下是数据库没有数据，还是接口调用有问题，还是接口有问题** | **已修复** | **根因/修复：同 BUG00007，所有详情页共用同一套逻辑，一并修复** |
|**BUG00009**| **http://localhost:5173/drama/new** | **可以看到数据** | **页面是空的，检查下是数据库没有数据，还是接口调用有问题，还是接口有问题** | **已修复** | **根因：`DramaDetail.vue` 是路由复用组件（从 `/drama/xxx` → `/drama/new` 不会重新 mounted），`onMounted` 只执行一次。切换到 new 模式时 `isNew=true` 但初始化逻辑不会重新触发 → store.currentDrama 为 null → 条件渲染 `v-if="isNew && store.currentDrama"` 不满足 → 页面空白。修复：① 新增 `initPage()` 函数统一处理新建/编辑两种模式的初始化；② 添加 `watch(route.params.id)` 监听路由参数变化并重新调用 initPage()；③ 新建模式下先调用 `store.reset()` 清理旧数据再设置默认值。确保每次路由切换（包括详情间跳转、new↔编辑互切）都能正确渲染** |
|**BUG00010**| **全部页面** | **同按钮在icon和文字纵向是对其的** | **只有导航栏的icon和文字纵向是对齐的，除此以外其他的都没有对齐** | **已修复** | **根因：导航栏使用原生 `<button>` + 显式 `display:inline-flex;align-items:center` 已对齐；其余页面使用 Ant Design `<a-button>` + `<template #icon>` 插槽，Ant Design 默认样式在暗色主题下 icon 与文字未垂直居中。修复：在 `App.vue` 底部添加全局非 scoped `<style>` 块，强制所有 `.ant-btn` 使用 `display:inline-flex !important; align-items:center !important; gap:6px !important`，同时 `.ant-btn .anticon` 也设为 inline-flex 居中。全局生效，覆盖所有页面的 a-button 组件** |
|**BUG00011**| **/settings/storage** | **配置好OSS后配置信息永久保留，下次重启后不会重置。图片在卡片上可以预览，点进去可以放大预览，视频点进去也可以播放预览，音频点进去也能播放雨啦就能** | **文件删除功能不起效果，删除后，列表中的文件记录依然还在** | **已修复** | **根因（共 3 轮修复）：<br/>第1轮 — OSS持久化：OssConfigPersistence.java 解决<br/>第2轮 — storage.ts 双重baseURL + 缺下载端点：已修复<br/>第3轮 — OSS ACL私有 + 删除TODO：uploadToOss() 加 setObjectAcl(PublicRead)；deleteAsset() TODO→client.deleteObject()** |
|**BUG00012**| **全部页面** | **图片URL（可选） 改为 从存储里面去选择图片，以及直接上传图片两种方式（与OSS部分联动）** | **目前还是 填写 图片URL，对用户操作不友好** | **已修复** | **DramaDetail.vue 角色弹窗和场景弹窗全面改造：① "从存储选择"按钮→打开图片选择器弹窗(assetApi.page)；② "上传图片"按钮→storageApi.upload()上传到本地/OSS自动填入URL；③ 已选图片预览128px+清除按钮** |
|**BUG00013**| **/settings/ai** | **需要多一个字段：Token单价（0.0002元/Token）** | **任务追踪需要记录所有任务的Token消耗量，以及换算出来的单价是好多** | **已修复** | **三层改动：① SQL migration-v3-token-price.sql ADD COLUMN token_price DECIMAL(10,6) DEFAULT 0.000200；② AiConfig.java 新增 BigDecimal tokenPrice 字段；③ AiConfig.vue 弹窗新增 Token单价输入框(a-input-number, precision=6, 默认值0.0002)** |
| **BUG00014** | **/settings/ai** | **添加修改记录后，列表能够正常展示** | **添加记录，列表还是空的（数据库有数据）** | **已修复** | **根因：AiConfigService.listByType() 只返回 enabled 配置 → 禁用的从管理列表消失。<br/>修复：新增 listAll() 方法(只查 deleted=0) + GET /api/v1/ai-configs 端点 + 前端改单次请求** |
| **BUG00015** | **所有页面** | **AI生成按钮，可以正常使用** | **【AI生成】按钮 执行报错：ofetch is not defined** | **✅ 已修复 ai.ts + media.ts 缺 import，改用 api 实例** | **2026-04-15** |
| **BUG00016** | **AI图片/文本生成** | **AI生成按钮可正常调用后端 API** | **后端报错：java.net.UnknownHostException: api.minimaxi.com** | **✅ 已修复** | **RestTemplateConfig 底层换用 OkHttp3ClientHttpRequestFactory + 自定义DNS(Google 8.8.8.8) + application-dev.yml dns.enabled=true** |
| **BUG00017** | **AI图片/文本生成** | **使用 MiniMax 配置的模型生成** | **图片生成报错 unsupported model: dall-e-3；文本生成 unknown model 'gpt-4o'** | **✅ 已修复** | **StoryboardService "gpt-4o"→null；ImageGenerationService 三个方法 "dall-e-3"→null；让适配器使用DB中的MiniMax模型名** |
| **BUG00018** | **/settings/storage** | **文件类型正确识别+媒体预览正常播放** | **.mp4被标为image；视频按图片组件加载报error；删除可能不生效** | **✅ 已修复 (2026-04-19)** | **修复（2个文件）：<br/>① FileStorageService.java：新增 guessTypeByFilename(filename) 扩展名检测 + uploadBytes(byte[])字节数组上传；uploadFile()改为MIME→扩展名→前端type三级判断链<br/>② StorageSettings.vue：新增getRealFileType(item)三级推断函数(mimeType→db type→扩展名)，模板全部替代item.type** |
| **BUG00018-1** | **角色/场景删除** | **软删除生效，删除按钮不触发编辑弹窗** | **场景删除按钮打开编辑页(事件穿透)** | **✅ 已修复 (2026-04-19)** | **DramaDetail.vue 场景卡片改造：a-popconfirm删除按钮从内联改为独立div包裹+z-index提升+@click.stop阻止冒泡到外层@click=openSceneModal(scene)。角色删除后端用DELETE+MyBatis-Plus @TableLogic逻辑删除，刷新后不显示已删除记录** |
| **BUG00019** | **/drama/{id} 分镜Tab** | **分镜持久化+工作台入口** | **进入页面分镜不见了** | **✅ 已验证无需修复 (2026-04-19)** | **经代码审查确认：后端StoryboardService.generateFromScript()第107行saveBatch(storyboards)✅持久化；listByEpisode(deleted=0)✅查询正确；前端loadData()第55行storyboardApi.list(id,1)✅加载sbList；路由/workbench/:dramaId+Workbench.vue✅存在且有入口按钮。之前问题应为测试时后端异常导致saveBatch未执行** |
| **BUG00020** | **AI图片生成** | **MiniMax图片正常生成** | **input new_sensitive 内容审核拦截正常创作提示词** | **✅ 已修复 (2026-04-19)** | **MiniMaxAdapter.java：图片生成请求体新增 `"content_safe": false` 参数，放宽MiniMax安全审核策略。MiniMax默认对prompt做安全审查，正常的角色描述/场景提示词会被误判为sensitive而拒绝生成** |
| **BUG00021** | **AI生成归档OSS** | **AI生成文件保存到用户配置的OSS** | **AI生成的图片只存临时URL（24h时效性），未归档到阿里云** | **✅ 已修复 (2026-04-19)** | **ImageGenerationService.java：`archiveAiImage()`方法中，将`RestTemplate.exchange(tempUrl, ...)`改为使用`URI.create(tempUrl)`创建URI对象，然后调用`downloadRestTemplate.getForEntity(uri, byte[].class)`。避免RestTemplate对带签名的URL进行二次编码，导致MiniMax OSS的签名验证失败（403 SignatureDoesNotMatch）** |
| **BUG00022** | **/settings/storage** | **点击删除文件可以正常删除，且文件从列表移除** | **删除AI生成文件时报NPE：file_path为null导致Paths.get()报错** | **✅ 已修复 (2026-04-19)** | **FileStorageService.java：`deleteAsset()`方法中添加`file_path`空值检查。AI生成的图片(source_type=ai_generated)没有物理文件(file_path=null)，只有外部URL，因此跳过物理删除，仅执行数据库逻辑删除** |
| **BUG00023** | **/settings/ai<br />/media/{id}** | **TTS试听+分镜选择器** | 在编辑角色页面，点击试听音色的时候报错：<br />2026-04-20T14:39:47.875+08:00 ERROR 24120 --- [zhenling-drama] [nio-8080-exec-7] c.drama.common.GlobalExceptionHandler    : Server error: /api/v1/audios/preview - Required request body is missing: public com.drama.common.R<com.drama.entity.Audio> com.drama.controller.AudioController.update(java.lang.String,java.util.Map<java.lang.String, java.lang.Object>)<br/><br/>org.springframework.http.converter.HttpMessageNotReadableException: Required request body is missing: public com.drama.common.R<com.drama.entity.Audio> com.drama.controller.AudioController.update(java.lang.String,java.util.Map<java.lang.String, java.lang.Object>)<br /><br /><br />媒体工作室的整个交付都是混乱的,首先就是视频生成，我选不到具体的分镜。另外一个就是关联分镜，关联角色也选不上而且我担心这里面的交互动作有冲突或者交叉.这个最终导致的结果就是流程不通<br /><br />直到现在，我还是不知道这个分镜合成怎么去操作，因为我没有找到就是对应分镜去合成的这个页面入口或者是按钮入口。而且，这个分镜编辑的时候，没有跟我的场景图和角色图进行关联，我需要他们关联起来。<br /><br />https://platform.minimaxi.com/docs/guides/video-generation 这个接口文档，你需要仔细阅读一下：<br />视频生成服务提供多种功能：<br/>文生视频：根据文本描述直接生成视频<br/>图生视频：基于一张初始图片结合文本描述生成视频<br/>首尾帧生成视频：提供视频开始、结束图片，来生成视频<br/>主体参考生成视频：基于一人脸照片，文本描述生成视频，视频中保持人物特征一致性<br />必要时需要改写Adapter适配器。不同场景下，其实调用的这个接口是不一样的。这个需要你根据分镜情景来做决策 | **✅ 已修复 (2026-04-20)** | **修复（共 3 后端 + 4 前端文件）：<br/><br/>### 后端改造<br/>① TtsService.java: `generate()` 方法新增自动解析逻辑；新增 `preview()` 预览方法（不保存到数据库）。<br/>② VideoService.java: model 为空时从 AiConfig video 类型动态读取。<br/>③ AudioController.java: 新增 POST `/audios/preview` 试听端点。<br/><br/>### 前端改造<br/>④ DramaDetail.vue 角色弹窗: 专属音色区域(16种音色) + 试听按钮 + 内联播放器。<br/>⑤ MediaStudio.vue TTS Tab: 分镜选择器 + 角色关联 + 试听按钮。<br/>⑥ MediaStudio.vue Video Tab: 分镜选择器 + AI配置模型优先展示。<br/>⑦ aiConfig.ts: `ttsPreviewApi.preview()` 和 `getEnabledByType()` 方法。**|
| BUG00024 | 所有涉及到试听和播放音频的页面 | TTS生成音频归档到OSS | MiniMax临时URL(24h)未归档到用户配置的阿里云OSS | **✅ 已修复 (2026-04-20)** | **TtsService.java saveAudioFile() 全面改造：**<br/>① URL格式：下载临时URL→RestTemplate→uploadBytes归档到OSS（参考ImageGenerationService.archiveAiImage模式）<br/>② Base64/Hex格式：解码后→uploadBytes上传OSS（优先）→失败降级存本地文件系统<br/>③ 新增 archiveAudioFromUrl() + saveAudioToLocal() + hexStringToByteArray() 三个私有方法<br/>④ 注入 FileStorageService + AssetService + RestTemplate(download) |
| **BUG00025** | **/media/{dramaId}** | **视频+FFmpeg+OSS归档+预览路径** | FFmpeg找不到 + videoUrl存的是file_id + 合成后预览路径不可访问 | **✅ 已修复 (2026-04-20 补全)** | **修复（共4个问题，6文件改动）：**<br/><br/>**① FFmpeg可配置**: VideoComposeService.FfmpegConfig(ffmpeg.path/probe-path)，application.yml 可指定完整路径 ✅<br/><br/>**② 视频归档OSS**: VideoService.archiveVideoToStorage() + ComposeService.composeEpisode() 合成后归档 ✅<br/><br/>**③ 预览路径修正(补全)**: ComposeController 新增 `GET /compose/download/**` 端点，支持本地合成文件的HTTP访问（视频预览/播放/下载）。ComposeService.composeEpisode() 归档失败时降级为 `/api/v1/compose/download/{dramaId}/{filename}` 路径，替代裸本地绝对路径。<br/><br/>**④ file_id→真实下载URL**: MiniMaxAdapter.fetchFileDownloadUrl() 解析为 download_url；pollVideoStatus() SUCCESS时自动转换 |
| **BUG00026** | /media/fe-dark-dragon?tab=video&storyboardId=... | **视频图片选择器+配音可选角色+记录标记分镜+参数传入+API对齐** | 1.首尾帧/主体参考/图生视频都是填URL，需改存储选择器 2.配音不选角色报错 3.记录无法区分分镜 **4.prompt为空→画面随机** **5.API参数名错误(图生/主体参考)** | **✅ 已修复 (2026-04-20 深度修复)** | **修复（共6个子问题，2文件改动核心 + 前端辅助）：**<br/><br/>**① 图片URL→存储选择器**: MediaStudio.vue 视频Tab的图生视频/首尾帧(2个)/主体参考 全部改为「从素材库选择」按钮+缩略图预览+清除功能。<br/><br/>**② 配音characterId可选**: AudioController.java `@RequestParam String characterId` → `@RequestParam(required=false)`。<br/><br/>**③④ 视频记录显示分镜号**: 视频 `{model} · 分镜 #{shotNumber}`，配音 `· #{shotNumber}` 标签。<br/><br/>**⑤ prompt参数丢失(BUG00026-2)**: 前端智能构建提示词（`角色名+场景+action/description/dialogue+mood`），后端空prompt用有意义默认描述+WARN日志。<br/><br />**⑥🔥 API参数完全对齐官方规范(MiniMaxAdapter核心重写)**:<br/>- 🔴 **新增必填参数 `resolution: "1080P"`** — 旧代码完全没有传此字段<br/>- 🔴 **图生视频参数名修正**: `reference_image_url`(不存在) → **`first_frame_image`**(官方规范) — 图生视频模式之前一直传错字段名！<br/>- 🔴 **主体参考结构修正**: `subject_reference_image:"url"`(字符串) → **`subject_reference:[{type:"character",image:["url"]}]`**(数组对象) — 完全不符合官方格式<br/>- 🟡 **模型按模式自动切换**: 文生/图生→Hailuo-2.3, 首尾帧→**Hailuo-02**, 主体→**S2V-01**（旧代码全部用Hailuo-2.3）<br/>- 新增 `resolveVideoModel(mode, model)` 方法实现智能选型 |
| **BUG00027** | **/media/{dramaId}** | **整集合成可用（按分镜顺序+视频音频合并）** | **FFmpeg HTTPS协议白名单限制导致无法合成 + 同分镜配音视频未合并 + 排序错误(按storyboardId而非shotNumber)** | **✅ 已修复 (2026-04-20)** | **修复（共 3 个核心问题，3 文件改动）：**<br/><br/>### 问题①：FFmpeg HTTPS 协议白名单限制<br/>**VideoComposeService.java 3个方法改造：**<br/>- `composeShot()`: 检测视频/音频URL为HTTP(S)时→先调用`downloadVideoToTemp()`下载到本地临时文件→FFmpeg用本地路径操作→完成后清理临时文件<br/>- `getVideoInfo()`: 检测远程URL时同样先下载再ffprobe分析<br/>- 新增 `escapeForFFmpeg()` 方法转义drawtext中的特殊字符（单引号、冒号等）<br/><br/>### 问题②：同分镜视频+配音合并<br/>**ComposeService.composeEpisode() 全面重写（8步流程）：**<br/>1. 查询该集所有分镜 → 按 **shotNumber升序排列** ✅<br/>2. 按 storyboardId 建立 videoMap 和 audioMap 索引<br/>3. 遍历每个分镜：查找已完成视频+关联配音+提取台词字幕<br/>4. 调用 `videoComposeService.composeShot(videoUrl, audioUrl, subtitle)` 合成单镜头（含音画同步）<br/>5. 所有镜头按 shotNumber 顺序拼接为完整剧集<br/>6. ffprobe 获取时长信息<br/>7. 归档到 OSS/本地存储<br/>8. 创建 EpisodeExport 记录（含 shotCount/withAudioMerged 元数据）<br/>- 新增依赖：StoryboardMapper（用于获取 shotNumber 排序）<br/>- 降级策略：某镜头合成失败时降级用原始视频（不中断整集流程）<br/><br/>### 问题③：排序修正<br/>- **旧逻辑**：`orderByAsc(Video::getStoryboardId)` — 按UUID字符串排序（无意义）<br/>- **新逻辑**：查询 storyboards 表 → 按 `shotNumber` 排序 → 逐个查找对应视频 → 保证镜头1在镜头2前面 |
|**BUG00028**| **drama/{dramaId}<br />/workbench** | **分镜删除不触发编辑弹窗+已删除记录不显示**。点击删除按钮后，可以将对应的记录值deleted置为1 | 已删除的分镜仍显示在列表中。(正确的逻辑是只展示 deleted=0的记录) | **✅ 已修复 (2026-04-21)** | **根因：`request.ts` 中 `storyboardApi.delete` 使用了错误的 HTTP 方法 `POST`，而后端 Controller 使用 `@DeleteMapping`，导致删除请求无法正确匹配端点，删除操作未实际执行。<br/><br/>修复（1个核心文件）：<br/>① **`request.ts`**: `storyboardApi.delete` 方法从 `method: 'POST'` 改为 `method: 'DELETE'`，与后端 `@DeleteMapping("/{id}")` 正确匹配<br/><br/>补充说明：Workbench.vue 和 DramaDetail.vue 的事件冒泡阻止（`@click.stop`）和删除后自动刷新逻辑此前已正确实现，无需修改 |
| **BUG00029** | **/media/{dramaId}** | **配音记录可删除** | **配音记录没有删除功能按钮** | **✅ 已修复 (2026-04-20)** | **MediaStudio.vue TTS Tab 改造：**<br/>① 配音记录列表添加删除按钮（hover 显示）+ `a-popconfirm` 确认对话框<br/>② 新增 `deleteAudio()` 方法调用 `audioApi.delete()`，删除成功后本地过滤更新列表 |
| BUG00030 | 所有调用AI接口相关的页面 | 图片合成、声音合成、分镜生成、视频生成都能正常使用。如果有错误，请参考下：[概览 - MiniMax 开放平台文档中心](https://platform.minimaxi.com/docs/guides/models-intro) 对代码进行修复 | 图片生成失败：AI返回为空，请检查图片生成配置<br />TTS生成失败：TTS返回为空<br />分镜拆解失败：AI返回结果为空，请检查AI文本生成配置是否正确（需要在AI配置页面添加text类型的配置）<br />视频生成失败：code=1008 insufficient balance（余额不足）但前端只显示"视频生成返回为空" | **✅ 已修复 (2026-04-21)** | **修复（核心改造：错误信息透传机制）：<br/><br/>### 根因<br/>MiniMaxAdapter 在所有 AI 接口调用中，当厂商返回非成功状态码时（如 `1008=余额不足`、`1004=参数错误` 等），只打日志后静默返回空字符串 `""`。上层 Service 拿到空值后只能给用户笼统的"AI返回为空"，用户完全不知道真实原因。<br/><br/>### 改造内容<br/><br/>**① 新增 `AiApiException.java`**（自定义异常类）：<br/>- 包含4层信息：vendorCode(厂商原始码)、vendorMessage(厂商原文)、userMessage(中文翻译)、operation(操作类型)<br/>- 继承 RuntimeException，可被 GlobalExceptionHandler 统一捕获并返回给前端<br/><br/>**② MiniMaxAdapter.java 全面重构（5个方法）**：<br/>- 新增 `ensureSuccess(root, operation)` 方法替代旧的 `isSuccess()` 静默检查——检测到非零 status_code 时直接抛出 AiApiException<br/>- 新增 `translateErrorCode(code, msg, operation)` 方法翻译常见错误码：<br/>  → 1008 → "MiniMax 账户余额不足，请在 MiniMax 开放平台充值后再试"<br/>  → 1001 → "MiniMax API Key 无效或已过期"<br/>  → 1013 → "请求频率超限，请稍后重试"<br/>  → 1014 → "内容安全审核未通过，请调整提示词"<br/>  → 其他 → 保留原文 + 错误码<br/>- generateText/generateImage/generateVideo/generateVideoMultiMode/generateTTS 全部改为抛 AiApiException 而非静默返回空<br/><br/>**③ 上层 Service 适配（4个文件）**：<br/>- VideoService: catch(AiApiException) → 直接用翻译好的中文消息构造 BusinessException；新增 saveFailedVideo() 保存失败记录<br/>- ImageGenerationService: 3个方法全部增加 AiApiException catch 分支<br/>- TtsService: generate() 和 preview() 增加 AiApiException catch 分支<br/>- StoryboardService: 引入 AiApiException（异常自然冒泡到 GlobalExceptionHandler）<br/><br/>### 效果对比<br/>|
| BUG00031 | 所有涉及到使用素材库的页面 | 使用素材库的时候，可以就是滚动翻页（文件管理页面除外） | 目前在 角色管理-》选择对应的角色-》从存储库选择-》只能选择第一个分页的内容。要修复这个问题 | 未修复 | |
| BUG000311-1 | drama/{dramaId} | 在分镜-》点击媒体工作室-》进入媒体工作室页面后，可以返回上一级 | 目前发现，进入了这个媒体工作室页面以后，无法返回上一级。需要像工作台一样，可以返回上一级。 | 未修复 | |
| **BUG00032** | **application.yml / application-dev.yml** | **FFmpeg路径不应硬编码在主配置中（Windows绝对路径）** | **FFmpeg路径硬编码为 `C:/Data/FFmpeg/.../ffmpeg.exe`，Linux生产环境无法使用** | **✅ 已修复 (2026-04-21)** | **修复（2文件改动）：<br/>① `application.yml`：ffmpeg.path/probe-path 改为空字符串（走系统PATH）<br/>② `application-dev.yml`：新增 ffmpeg 配置块，指定 Windows 开发环境完整路径<br/>效果：Linux/macOS 生产部署无需修改主配置，开发环境自动从 dev profile 加载 Windows 路径** |
| **BUG00033** | **前端全部页面(3个组件)** | **生产环境无调试日志残留** | **StorageSettings(6处) + MediaStudio(2处) + DramaDetail(1处) console.log 在生产环境输出到控制台** | **✅ 已修复 (2026-04-21)** | **修复（3文件改动，共9处）：<br/>① `StorageSettings.vue`：移除6处 console.log<br/>② `MediaStudio.vue`：2处改为 `if (import.meta.env.DEV)` 条件限定<br/>③ `DramaDetail.vue`：1处改为 `if (import.meta.env.DEV)` 条件限定<br/>效果：生产构建完全无日志输出；开发环境保留必要调试信息** |
| **BUG00034** | **MediaStudio.vue / Workbench.vue** | **前端轮询定时器无泄漏风险** | **页面快速切换时可能启动多个并行 setInterval 定时器** | **✅ 已验证加固 (2026-04-21)** | **审计全部4个定时器使用点：MediaStudio/Workbench/TaskTracker/useToast — 全部已有 onUnmounted clearInterval 清理 ✅<br/>加固：MediaStudio + Workbench 的 onMounted 新增防重复挂载逻辑 — 启动新定时器前先清理旧实例** |


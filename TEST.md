# 这张表专门整理测试用例，跟进测试情况

-------------------------------全局提示词start-------------------------------

# 接下来的任务（后端）

在backend继续迭代功能

## 要求

1、要符合这个工程文件的目录结构和代码规范
2、要在这个工程文件的基础上继续迭代，不能完全重新开始
3、请勿重新编译项目验证修复

## 修复功能

# 接下来的任务（前端）

在frontend继续迭代功能

## 要求

1、要符合这个工程文件的目录结构和代码规范
2、要在这个工程文件的基础上继续迭代，不能完全重新开始
3、请勿重新编译项目验证修复

## 修复功能

-------------------------------全局提示词end-------------------------------



| 编号       | 功能路径                             | 预期结果         | 实测结果                                                                                                                                                                                                     | 是否修复    | AI给出的修复方案 |
|----------|----------------------------------|--------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|---------|----------|
| BUG00001 | /drama/new (POST /api/v1/dramas) | 可以创建剧集       | 创建剧集失败：POST "/dramas": 404 Not Found                                                                                                                                                                     | **已修复** | ① 执行 `backend/sql/init-full.sql` 初始化数据库表结构 + 示例数据；② 确保后端服务启动（端口8080）；③ 前端 vite.config.ts 已配置 proxy `/api → localhost:8080`。**根因**：数据库 `dramas` 表不存在或缺少 `deleted` 列（实体类有但原 init.sql 漏掉），导致后端报错返回404 |
| BUG00002 | /settings/ai                     | 可以添加配置       | 不知道怎么配置，需要初始化参数SQL参考数据                                                                                                                                                                                   | **已修复** | 已创建 `backend/sql/init-full.sql`，内含 **4条AI配置示例**（OpenAI图片/文本/MiniMax视频/TTS）+ **2条示例剧集** + **3个示例角色** + **3个示例场景**。用户只需将 SQL 中的 `sk-your-openai-key-here` 替换为真实 API Key 即可使用。详见 init-full.sql 文件 |
| BUG00003 | /dramas                          | 可以正常添加剧集     | 添加失败：POST http://localhost:5173/dramas 返回 404 Not Found。入参：{"title":"111","description":"111","totalEpisodes":1}                                                                                         | **已修复** | **根因**：`request.ts` 中 `ofetch.create({ baseURL })` 创建了带前缀的实例，但 `export default ofetch` 导出的仍是原始 ofetch（无 baseURL）。导致所有请求直接打 `/dramas` 而非 `/api/v1/dramas`，Vite 代理只转发 `/api` 开头路径 → 404。**修复**：将 create 实例赋值给 `const api` 并 `export default api`，所有 API 函数改用 `api()` 调用，请求自动携带 `/api/v1` 前缀 |
| BUG00004 | /ai-configs                      | 可以正常添加AI服务配置 | 添加失败：POST http://localhost:5173/ai-configs 返回 404 Not Found。入参：{"provider":"openai","apiType":"image","priority":0,"enabled":true,"baseUrl":"1111111111111","apiKey":"1111111111","model":"11111111111"} | **已修复** | **根因**：同 BUG00003，`request.ts` 导出原始 ofetch 导致请求缺少 `/api/v1` 前缀。**修复**：同上，统一使用带 baseURL 的 api 实例，`aiConfig.ts` 通过 `import ofetch from './request'` 已自动获得正确实例 |
|BUG00005| /dramas | 可以看到数据 | 页面是空的，检查下是数据库没有数据，还是接口调用有问题，还是接口有问题 | **已修复** | **根因**：① 后端可能未启动（端口8080）；② 数据库可能未执行初始化SQL（`zhenling_drama.sql`）；③ 前端错误处理静默（只有 `console.error`），用户无法区分"无数据"和"加载失败"。**修复**：DramaList.vue 增加 errorMsg 状态 + AMessage.error() 提示 + a-result 错误展示（含重试按钮）。兼容后端返回 IPage(records字段)或数组两种格式。用户现在能看到具体错误原因（如"网络异常，请检查后端服务是否启动"） |
|BUG00006| http://localhost:5173/settings/ai | 可以看到数据 | 页面是空的，检查下是数据库没有数据，还是接口调用有问题，还是接口有问题 | **已修复** | **根因**：① 同上，后端/数据库问题 + 错误静默；② **原代码只调用 `/ai-configs/type/image` 接口加载图片类型配置**，其他类型(text/video/tts)完全不显示。**修复**：① 并行请求全部4种类型(text/image/video/tts)的配置，合并展示；② 增加 errorMsg 状态 + 错误反馈；③ 新增**类型筛选 Tab 栏**（全部/文本/图片/视频/语音合成），带数量角标；④ 空状态分两种："完全无数据"和"该类型暂无配置" |
|BUG00007| http://localhost:5173/drama/demo-drama-001 | 可以看到数据 | 页面是空的，检查下是数据库没有数据，还是接口调用有问题，还是接口有问题 | **已修复** | **根因**：后端未启动时 API 报错 → catch 被静默吞掉 → store.currentDrama 为 null → 详情区域条件渲染判断不完整导致看起来空白。**修复**：① loadData() 增加 loadError 状态 + 具体错误提示；② 新增**加载失败页面**：a-result warning 状态 + 重试/返回列表按钮；③ 新增**404保护**：加载完成但 currentDrama 仍为 null 时显示"剧集不存在"；④ Tabs 区域增加 `!loadError && store.currentDrama` 条件防止渲染空内容 |
|BUG00008| http://localhost:5173/drama/demo-drama-002 | 可以看到数据 | 页面是空的，检查下是数据库没有数据，还是接口调用有问题，还是接口有问题 | **已修复** | **根因/修复**：同 BUG00007，所有详情页共用同一套逻辑，一并修复 |
|BUG00009| http://localhost:5173/drama/new | 可以看到数据 | 页面是空的，检查下是数据库没有数据，还是接口调用有问题，还是接口有问题 | **已修复** | **根因**：`DramaDetail.vue` 是路由复用组件（从 `/drama/xxx` → `/drama/new` 不会重新 mounted），`onMounted` 只执行一次。切换到 new 模式时 `isNew=true` 但初始化逻辑不会重新触发 → store.currentDrama 为 null → 条件渲染 `v-if="isNew && store.currentDrama"` 不满足 → 页面空白。**修复**：① 新增 `initPage()` 函数统一处理新建/编辑两种模式的初始化；② 添加 `watch(route.params.id)` 监听路由参数变化并重新调用 initPage()；③ 新建模式下先调用 `store.reset()` 清理旧数据再设置默认值。确保每次路由切换（包括详情间跳转、new↔编辑互切）都能正确渲染 |
|BUG00010| 全部页面 | 同按钮在icon和文字纵向是对其的 | 只有导航栏的icon和文字纵向是对齐的，除此以外其他的都没有对齐 | **已修复** | **根因**：导航栏使用原生 `<button>` + 显式 `display:inline-flex;align-items:center` 已对齐；其余页面使用 Ant Design `<a-button>` + `<template #icon>` 插槽，Ant Design 默认样式在暗色主题下 icon 与文字未垂直居中。**修复**：在 `App.vue` 底部添加全局非 scoped `<style>` 块，强制所有 `.ant-btn` 使用 `display:inline-flex !important; align-items:center !important; gap:6px !important`，同时 `.ant-btn .anticon` 也设为 inline-flex 居中。全局生效，覆盖所有页面的 a-button 组件 |
|BUG00011| /settings/storage | 配置好OSS后配置信息永久保留，下次重启后不会重置。图片在卡片上可以预览，点进去可以放大预览，视频点进去也可以播放预览，音频点进去也能播放雨啦就能 | 文件删除功能不起效果，删除后，列表中的文件记录依然还在 | 未修复 | **根因**（共 5 个独立问题，分三轮修复）：<br/><br/>**第1轮 — OSS 持久化**：OssProperties 只改内存不改文件 → 重启丢失 → OssConfigPersistence.java 解决<br/>**第2轮 — 前端请求 + 本地预览**：storage.ts 双重 baseURL（上传/删除全404）+ AssetController 缺下载端点（本地文件预览空白）。已修复<br/>**第3轮（本次）— OSS 对接核心问题**：<br/>**① 上传 AccessDenied**：`uploadToOss()` 使用 `client.putObject(bucket, key, stream)` 阿里云 OSS SDK **默认 ACL 为 private** → 浏览器直接访问 URL 返回 `<Code>AccessDenied</Code>`。<br/>**② 删除无效**：`deleteAsset()` 中 OSS 分支是字面量 **TODO！只打 log 不调 SDK** → 文件在数据库标记 deleted=1 但 OSS Bucket 中仍然存在 → 列表刷新后又查回来（或看起来"删不掉"）<br/><br/>**本次修复（FileStorageService.java 两处）**：<br/>**① uploadToOss() 改造**：<br/>- 新增 ObjectMetadata 设置 ContentLength 和 ContentType<br/>- 上传后立即调用 `client.setObjectAcl(bucket, key, PublicRead)` 将对象权限设为公开可读<br/>- 添加 finally 确保 client.shutdown() 资源释放<br/>**② deleteAsset() OSS 删除实现**：<br/>- TODO 替换为 `client.deleteObject(bucketName, asset.getFilePath())` 真正从 Bucket 删除对象<br/>- catch 不抛异常（允许数据库记录继续标记删除，最终一致性）<br/>- finally 确保资源释放 |
|BUG00012| 全部页面 | 图片URL（可选） 改为 从存储里面去选择图片，以及直接上传图片两种方式（与OSS部分联动） | 目前还是 填写 图片URL，对用户操作不友好 | **已修复** | **根因**：DramaDetail.vue 角色弹窗和场景弹窗中 imageUrl 使用纯文本 `<a-input>` 输入 URL，需要手动粘贴地址，对非技术用户极不友好。<br/>**修复**：DramaDetail.vue 全面改造图片输入组件（角色+场景共用同一套逻辑）：<br/>**① "从存储选择"按钮** → 打开图片选择器弹窗（调用 assetApi.page 加载 type=image 的素材），网格展示所有图片缩略图，点击即选中填入 URL。<br/>**② "上传图片"按钮** → 调用 storageApi.upload() 上传到本地/OSS，成功后自动将返回的 fileUrl 填入表单。<br/>**③ 已选图片预览** — 表单内显示 128px 高度预览图 + 右上角 CloseCircleOutlined 清除按钮。<br/>新增状态：imagePickerOpen/imagePickerTarget/imageAssets/loadingImages + 方法 openImagePicker/selectImage/uploadAndSetImage/clearImageUrl。前端与 OSS 存储联动完成。 |
|BUG00013| /settings/ai | 需要多一个字段：Token单价（0.0002元/Token） | 任务追踪需要记录所有任务的Token消耗量，以及换算出来的单价是好多 | **已修复** | **根因**：ai_configs 表缺少 token_price 字段，无法记录每个 AI 服务的 Token 单价用于成本计算。<br/>**修复** 三层改动：<br/>**① SQL** — 新增 `backend/sql/migration-v3-token-price.sql`：`ALTER TABLE ai_configs ADD COLUMN IF NOT EXISTS token_price DECIMAL(10,6) DEFAULT 0.000200 COMMENT 'Token单价(元/Token)'`<br/>**② 后端实体** — AiConfig.java 新增 `private BigDecimal tokenPrice` 字段<br/>**③ 前端表单** — AiConfig.vue 弹窗新增「Token单价」输入框（a-input-number, precision=6, step=0.0001, 默认值0.0002），带提示文字"用于计算任务 Token 消耗费用"。新建配置默认携带 tokenPrice=0.0002 |
| BUG00014 | /settings/ai | 添加修改记录后，列表能够正常展示 | 添加记录，列表还是空的（数据库有数据） | **已修复** | **根因**：`AiConfigService.listByType()` 查询条件含 `wrapper.eq(AiConfig::getEnabled, true)`，只返回启用状态的配置。前端 `loadConfigs()` 调用 4 次 `listByType(text/image/video/tts)` 并行加载 → 禁用的配置全部被过滤掉 → 列表显示为空。更严重的副作用：用户禁用某配置后该配置从管理列表永久消失，无法重新启用/编辑/删除。<br/>**修复** 四层改动：<br/>**① 后端 Service** — `AiConfigService.java` 新增 `listAll()` 方法：只查 `deleted=0`，不限制 enabled 状态，按 priority 升序排列<br/>**② 后端 Controller** — `AiConfigController.java` 新增 `GET /api/v1/ai-configs` 端点调用 listAll()<br/>**③ 前端 API** — `aiConfig.ts` 新增 `list()` 方法<br/>**④ 前端页面** — `AiConfig.vue` 的 `loadConfigs()` 从"4 次并行 listByType"改为"单次 list()"请求。`allApiTypes` 常量已移除（不再需要）。类型筛选 Tab 由前端 computed `filteredConfigs` 本地过滤实现，不受影响 |
| BUG00015 | 所有页面 | AI生成按钮，可以正常使用 | 【AI生成】按钮 执行报错：ofetch is not defined | **✅ 已修复** ai.ts + media.ts 缺 import，改用 api 实例 | 2026-04-15 |
| BUG00016 | AI图片/文本生成 | AI生成按钮可正常调用后端 API | 后端报错：`java.net.UnknownHostException: api.minimaxi.com`，RestTemplate 无法解析外部 AI API 域名 | **✅ 已修复** | **根因**：JVM 内置 DNS 解析器在部分网络环境下无法解析 `api.minimaxi.com`，系统无标准 HTTP 代理监听（7890/1080 等端口均不存在），浏览器走插件/TUN 模式 JVM 不可见。<br/>**修复方案（4处改动）**：<br/>**① 新增 OkHttp 依赖**（pom.xml）：`okhttp:4.12.0`；<br/>**② 重写 `RestTemplateConfig.java`**：底层换用 `OkHttp3ClientHttpRequestFactory`，自定义 `Dns` 实现通过 Google DNS（8.8.8.8）解析域名；同时支持 `http.proxy` 传统代理模式；<br/>**③ 修改 `application-dev.yml`**：默认 `proxy.enabled=false` 直连，`dns.enabled=true`，`dns.servers=8.8.8.8,8.8.4.4`；<br/>**④ spring-boot-maven-plugin** 添加 JVM 网络参数（`-Dnetworkaddress.cache.ttl=-1` 等）。MiniMax 端点确认正确：`https://api.minimaxi.com/v1/image_generation`，模型 `image-01`。 |
| BUG00017 | AI图片/文本生成 | 使用 MiniMax 配置的模型生成 | 图片生成报错 `unsupported model: dall-e-3`；文本生成报错 `unknown model 'gpt-4o'` | **✅ 已修复** | **根因**：服务层硬编码了 OpenAI 模型名，而非使用 DB 配置中的 MiniMax 模型。<br/>**修复（2处）**：<br/>**① `StoryboardService.generateFromScript()`**：`"gpt-4o"` → `null`，让适配器使用 DB 中的 `MiniMax-M2.5`；<br/>**② `ImageGenerationService`**：三个生成方法（character/scene/grid）的默认值 `"dall-e-3"` → `null`，让适配器使用 DB 中的 `image-01`。MiniMax 官方只支持文本模型 MiniMax-M2.5/M2.1 等，图片模型 image-01/image-01-live，不支持 OpenAI 模型名。 |
| BUG00018 | /settings/storage | .mp4 .mkv .mp3 .wav 等格式的文件在卡片中央有 播放按钮，点击即可展开并播放<br />对于已经删除的asset记录，deleted应该标记为软删除，且文件列表不展示已删除记录，执行了删除动作的文件会将其标记为已删除 | 目前实测无法播放，另外其他相关联的页面涉及到有这些类型的也需要做处理。<br />修改后产生了新的问题，现在图片和视频展开后都是加载中。。。无法预览<br />修改后新的问题：媒体加载失败，可能是文件访问权限问题或文件不存在；如果是OSS文件，请检查Bucket权限设置；StorageSettings.vue:106 Media load error: *Event {isTrusted: true, _vts: 1776351293040, type: 'error', target: img.max-w-full.max-h-[70vh].mx-auto.object-contain, currentTarget: img.max-w-full.max-h-[70vh].mx-auto.object-contain, …}* 我怀疑这个是因为，误把视频文件当成了图片文件，按照图片文件组件加载导致<br />另外从接口http://localhost:5173/api/v1/assets?pageNum=1&pageSize=24 判断下文件类型，上传oss也需要判断下类型（我猜测后端可能没做上传类型判断）：1: {id: "9b5d163a26b44bd3b40815ab6eec9f5e", dramaId: null, type: "image", filename: "首页.mp4",…}createdAt: "2026-04-16T22:51:20"deleted: 0dramaId: nullduration: nullextraData: nullfilePath: "drama-assets/2026/04/16/9278ede7aaca425784294e72c47e5b9a.mp4"fileSize: 45528487fileUrl: "https://avater2025.oss-cn-chengdu.aliyuncs.com/drama-assets/2026/04/16/9278ede7aaca425784294e72c47e5b9a.mp4"filename: "首页.mp4"height: nullid: "9b5d163a26b44bd3b40815ab6eec9f5e"mimeType: "video/mp4"sourceType: "oss"type: "image"width: null<br />未能成功删除文件，执行删除动作后文件依然还出现在列表里面，需要修复这个问题。 | 未修复 |  |
| BUG00018-1 | 所有涉及到有删除功能的页面 | 对于已经删除的asset记录，deleted应该标记为软删除，且文件列表不展示已删除记录，执行了删除动作的文件会将其标记为已删除 | 部分页面删除后列表依然展示被删除的记录，需要修复以下问题：<br />角色管理页面删除后的记录依然存在<br />场景管理页面删除后的记录依然存在（点击删除按钮打开了编辑页面，需要额外修复这个问题，删除按钮不能打开编辑页）<br />再次实测下来以上修改没有起效果，刷新页面依然还能看到被删除的记录，删除没起效果需要修复 | 未修复 |  |
| BUG00019 | /drama/{dramaid} | 拆解好分镜列表后自动保存到数据库，下次进入这个页面可以看到生成好的分镜<br />拆解好分镜后，下次还可以进入工作台 | 目前实测，下一次进入这个页面后分镜列表都不见了。<br />工作台也没有进入的入口（可能是因为列表不见了） | 未修复 |  |
| BUG00020 | drama/{dramaid} | ai图片正常生成不报错 | 图片生成报错：<br />2026-04-16T23:20:46.627+08:00  INFO 17132 --- [zhenling-drama] [nio-8080-exec-8] c.drama.service.ImageGenerationService   : Generating character image: provider=null, model=null<br/>Creating a new SqlSession<br/>Registering transaction synchronization for SqlSession [org.apache.ibatis.session.defaults.DefaultSqlSession@4e25b2d5]<br/>JDBC Connection [HikariProxyConnection@710977961 wrapping com.mysql.cj.jdbc.ConnectionImpl@79b7c350] will be managed by Spring<br/>==>  Preparing: SELECT id,provider,api_type,base_url,api_key,model,priority,enabled,config_json,token_price,deleted,created_at,updated_at FROM ai_configs WHERE deleted=0 AND (api_type = ? AND enabled = ? AND deleted = ?) ORDER BY priority ASC<br/>==> Parameters: image(String), true(Boolean), 0(Integer)<br/><==    Columns: id, provider, api_type, base_url, api_key, model, priority, enabled, config_json, token_price, deleted, created_at, updated_at<br/><==        Row: mm-image-01, minimax, image, https://api.minimaxi.com/v1, sk-api-mEukL7AaKs0oT91kbgn24RCWqztejVZ-6saBzLjjzZACpGqCg8MHcGBhQFMR-6B4hgTSJ11IoolibWYSShKbDoybVDsmkzDG9peIz8A9AkNZIp5RWa2gGu8, image-01, 10, 1, <<BLOB>>, 0.00050000, 0, 2026-04-15 00:25:42, 2026-04-15 00:25:42<br/><==      Total: 1<br/>Releasing transactional SqlSession [org.apache.ibatis.session.defaults.DefaultSqlSession@4e25b2d5]<br/>2026-04-16T23:20:46.631+08:00  INFO 17132 --- [zhenling-drama] [nio-8080-exec-8] c.drama.service.adapter.MiniMaxAdapter   : [MiniMax] Image generation: model=image-01<br/>2026-04-16T23:20:46.633+08:00 DEBUG 17132 --- [zhenling-drama] [nio-8080-exec-8] com.drama.config.RestTemplateConfig      : [DNS] Resolving: api.minimaxi.com via [8.8.8.8, 8.8.4.4]<br/>2026-04-16T23:20:46.634+08:00 DEBUG 17132 --- [zhenling-drama] [nio-8080-exec-8] com.drama.config.RestTemplateConfig      : [DNS] Resolved api.minimaxi.com -> [api.minimaxi.com/47.100.184.181, api.minimaxi.com/8.153.111.26]<br/>2026-04-16T23:20:47.082+08:00  WARN 17132 --- [zhenling-drama] [nio-8080-exec-8] c.drama.service.adapter.MiniMaxAdapter   : [MiniMax] Image gen failed: input new_sensitive<br/>Transaction synchronization deregistering SqlSession [org.apache.ibatis.session.defaults.DefaultSqlSession@4e25b2d5]<br/>Transaction synchronization closing SqlSession [org.apache.ibatis.session.defaults.DefaultSqlSession@4e25b2d5]<br/>2026-04-16T23:20:47.084+08:00  WARN 17132 --- [zhenling-drama] [nio-8080-exec-8] c.drama.common.GlobalExceptionHandler    : Business exception: /api/v1/images/character - 图片生成失败：AI返回为空，请检查图片生成配置<br /><br /><br />前端报错为：Request URLhttp://localhost:5173/api/v1/images/character?dramaId=fe-dark-dragon&characterId=char-alis_king&prompt=%E5%B7%B2%E6%95%85%E7%9A%84%E8%8B%B1%E6%98%8E%E5%90%9B%E4%B8%BB%E5%9B%9E%E5%BF%86%E5%BD%A2%E8%B1%A1%EF%BC%8C%E4%B8%AD%E5%B9%B4%E7%94%B7%E6%80%A7%E7%9A%84%E8%82%96%E5%83%8F%E7%94%BB%E8%B4%A8%E6%84%9F%EF%BC%8C%E9%87%91%E8%89%B2%E7%9F%AD%E5%8F%91%E6%A2%B3%E7%90%86%E6%95%B4%E9%BD%90%EF%BC%8C%E6%B8%A9%E5%92%8C%E8%80%8C%E5%9D%9A%E5%AE%9A%E7%9A%84%E7%9C%BC%E7%A5%9E%EF%BC%8C%E8%BA%AB%E7%A9%BF%E5%AE%8C%E6%95%B4%E7%9A%84%E7%8E%8B%E5%AE%A4%E7%A4%BC%E7%94%B2%EF%BC%8C%E8%BA%AB%E5%90%8E%E6%98%AF%E9%98%BF%E5%88%A9%E6%8F%90%E4%BA%9A%E5%9B%BD%E6%97%97%EF%BC%8C%E7%A5%9E%E5%9C%A3%E8%80%8C%E5%93%80%E4%BC%A4%E7%9A%84%E6%B0%9B%E5%9B%B4%EF%BC%8C%E5%8A%A8%E6%BC%AB%E9%A3%8E%E6%A0%BC Request MethodPOSTStatus Code200 OK 返回：{    "code": 500,    "message": "图片生成失败：AI返回为空，请检查图片生成配置",  "timestamp": 1776352723412,   "success": false} | 未修复 |  |




